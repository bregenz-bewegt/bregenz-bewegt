import { clientCommonValidation } from './client-common-validation';

describe('clientCommonValidation', () => {
  it('should work', () => {
    expect(clientCommonValidation()).toEqual('client-common-validation');
  });
});
