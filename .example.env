# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

NX_DATABASE_URL="mysql://{username}:{password}@localhost:3306/bregenz_bewegt?connect_timeout=300"
NX_JWT_ACCESS_TOKEN_SECRET="{some-secret}"
NX_JWT_REFRESH_TOKEN_SECRET="{some-secret}"
NX_JWT_PASSWORD_RESET_TOKEN_SECRET="{some-secret}"
NX_JWT_EMAIL_RESET_TOKEN_SECRET="{some-secret}"
NX_CLIENT_BASE_URL="http://localhost:4200"
NX_API_BASE_URL="http://{your-ip-address}:3333" # Don't use "localhost" but an actual ip address for development since apps run on different hosts
NX_API_PORT=3333
NX_UPLOADS_FOLDER="uploads"
NX_MAIL_FROM="bregenz.bewegt@gmail.com"
NX_MAIL_PASSWORD="{some-password}"
NX_MAIL_AUTH_USER="{some-auth-user}"
NX_MAIL_AUTH_PASS="{some-auth-pass}"
NX_MAIL_HOST="{some-host}"
NX_MAIL_PORT=465
